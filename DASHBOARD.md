# QuantLab Web Dashboard

基于 Streamlit 构建的交互式 Web 界面，提供可视化的量化交易研究体验。

## 🚀 快速启动

### 方法 1: 使用 pip 启动（推荐）

```bash
# 安装依赖
pip install streamlit plotly

# 启动 Dashboard
streamlit run app.py

# 或使用安装的命令
quantlab-dashboard
```

### 方法 2: 使用 Python 直接运行

```bash
python -m streamlit run app.py
```

### 方法 3: 指定端口

```bash
streamlit run app.py --server.port 8501
```

## 📊 功能特性

### 1. 仪表盘 (Dashboard)
- 📊 概览统计：策略数量、回测次数、数据源、引擎
- 🚀 快速操作：新建策略、运行回测、参数优化
- 📋 最近结果：查看最近的回测结果摘要

### 2. 策略库 (Strategy Library)
- 📚 查看所有可用的策略配置
- 🔍 预览策略详情
- 🎯 一键运行回测

### 3. 创建策略 (Create Strategy)
- ➕ 可视化创建新策略
- 📝 表单化配置输入
- ✅ 自动验证和保存配置

支持配置项：
- 策略基本信息（名称、资产类型、标的）
- 数据设置（时间范围、频率）
- 回测参数（初始资金、佣金、滑点）
- 策略参数（策略类型、阈值）

### 4. 运行回测 (Backtest)
- 📈 交互式回测运行
- ⚙️ 回测选项配置
- 📊 实时结果展示
- 🏆 自动性能评级

回测选项：
- 回测引擎选择（VectorBT/Backtrader）
- 分块处理开关
- 内存优化配置

### 5. 参数优化 (Optimization)
- ⚙️ 贝叶斯优化界面
- 📊 参数范围设置
- 🎯 优化目标选择
- 📈 参数重要性分析

### 6. 报告 (Reports)
- 📑 生成策略报告
- 📄 多格式导出（PDF/HTML）
- 📈 图表可视化
- ⚠️ 风险警告

## 🎨 界面特点

- **响应式布局**: 自适应不同屏幕尺寸
- **交互式图表**: 使用 Plotly 实现动态可视化
- **实时反馈**: 任务进度实时更新
- **会话管理**: 自动保存会话状态

## 📁 文件结构

```
quantlab/
├── app.py                      # Streamlit 应用主文件
├── configs/examples/            # 策略配置示例
├── results/                    # 回测结果存储
└── data/                       # 数据存储目录
```

## 🔧 配置

### Streamlit 配置

在 `.streamlit/config.toml` 中创建配置：

```toml
[theme]
base="light"
primaryColor="#1f77b4"
backgroundColor="#ffffff"
secondaryBackgroundColor="#f0f2f6"
textColor="#262730"
font="sans serif"

[server]
headless=true
port=8501
enableCORS=false
```

### 环境变量

```bash
# 设置数据目录
export QUANTLAB_DATA_DIR="./data"

# 设置结果目录
export QUANTLAB_RESULTS_DIR="./results"

# 设置缓存目录
export QUANTLAB_CACHE_DIR="./cache"
```

## 🎯 使用流程

### 典型工作流

1. **创建策略**
   ```
   仪表盘 → 创建策略 → 填写表单 → 保存配置
   ```

2. **运行回测**
   ```
   策略库 → 选择策略 → 运行回测 → 查看结果
   ```

3. **参数优化**
   ```
   运行回测 → 参数优化 → 设置范围 → 运行优化 → 查看最佳参数
   ```

4. **查看报告**
   ```
   报告 → 选择策略 → 查看详情 → 导出报告
   ```

## 📊 性能评级系统

| 评级 | 条件 | 建议 |
|------|------|------|
| 🟢 A | 夏普>2, 卡尔马>2, 回撤<15% | 优秀，可实盘部署 |
| 🟡 B | 夏普1-2, 卡尔马1-2, 回撤15-25% | 良好，小仓位试运行 |
| 🟠 C | 夏普0.5-1, 卡尔马0.5-1, 回撤25-35% | 一般，继续优化 |
| 🔴 D | 夏普0-0.5, 卡尔马0-0.5, 回撤35-50% | 较差，重新设计 |
| ⚫ F | 夏普<0, 卡尔马<0, 回撤>50% | 失败，彻底放弃 |

## 🚀 生产部署

### Docker 部署

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

### 云平台部署

**Streamlit Cloud**
```bash
# 推送到 GitHub
git push origin main

# 在 streamlit.io 连接仓库
```

**Heroku**
```bash
heroku create quantlab-dashboard
git push heroku master
```

## 🔐 安全考虑

- 本地运行默认只绑定 `localhost`
- 生产环境请配置反向代理（Nginx/Apache）
- 建议添加身份验证机制
- 敏感数据使用环境变量

## 📈 扩展功能

### 计划中的功能

- [ ] 实时行情监控
- [ ] 订单管理界面
- [ ] 风险管理面板
- [ ] 策略比较工具
- [ ] 自动化部署
- [ ] 用户权限管理
- [ ] WebSocket 实时更新

## 🐛 故障排除

### 常见问题

**Q: 无法启动 Streamlit**
```bash
# 检查安装
pip show streamlit

# 重新安装
pip install --upgrade streamlit
```

**Q: 图表不显示**
```bash
# 检查 Plotly 安装
pip install --upgrade plotly

# 清除缓存
rm -rf ~/.streamlit/cache
```

**Q: 数据加载失败**
```bash
# 检查数据目录
ls -la data/

# 检查权限
chmod -R 755 data/
```

## 📞 技术支持

- GitHub Issues: https://github.com/NNBotForAI/quantlab/issues
- 文档: https://docs.quantlab.ai

---

**基于 QuantLab 平台，提供现代化、易用的量化交易研究界面。** 🚀
